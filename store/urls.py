# from django.urls import path 
# from . import views
# urlpatterns= [
    # path('<slug:category_slug>/',views.store,name='products_by_category'),
    # path('<slug:category_slug>/<slug:subcategory_slug>/',views.sub_categories,name='products_by_subCategory'),
    # path('<slug:category_slug>/<slug:subcategory_slug>/<slug:pro_slug>/',views.product_detail,name='product_detail'),
    # path('cart/',views.cart,name='cart'),
    # path('add_cart/<int:product_id>/',views.add_cart)
# ]    